#!/usr/bin/env bash
set -euo pipefail

project_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$project_root"

alr exec -- gprbuild -P tests/hra_n_tests.gpr -p
exec "$project_root/tests/bin/test_runner"
